The main code for this work is in the folder "MA".

The "train_net.py" is used for training the network.

"PD_demo.py" is used to evaluate the training results.
"evaluation.py" is used to work with "PD_demo.py".